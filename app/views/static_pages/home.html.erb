<div class="imagebox">
	<div class='imagecontainer'>
		<% @products.each  do |product| %>
			<div><%= link_to(product_path(product)) do %>
				<%= image_tag(product.photo_url, class: "image") %>
				<p><%= product.title %></p>
			<% end %></div>
		<% end %>
	</div>
</div>
